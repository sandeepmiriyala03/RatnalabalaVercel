# api/requirements.txt
#
# Down to a single dependency. LangChain + numpy + pydantic were
# adding hundreds of MB for a task that's really just two HTTP calls
# and a similarity comparison — none of which needs a framework.

requests==2.32.3